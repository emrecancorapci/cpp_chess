#include "Logger.h"

void Logger::log_move(const Vector2& home, const Vector2& target)
{
	//std::pair<Vector2, Vector2> kvp = {{home.x,home.y}, {target.x,target.y}};

	//_moves.insert(kvp);
}

// TODO : Log output
//std::string Logger::log_get_all()
//{
//	std::string response = "[ ";
//
//	for (auto kvp : _moves)
//	{
//		response += kvp.first.x + "," + kvp.first.y + " to " + kvp.second;
//	}
//}
